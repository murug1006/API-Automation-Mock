{
  "name": "tests",
  "version": "1.0.0",
  "description": "project for storing postman tests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Murugan",
  "license": "ISC",
  "dependencies": {
    "newman": "^4.0.2"
  }
}
